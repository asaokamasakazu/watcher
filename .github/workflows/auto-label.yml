name: Auto Label

on: pull_request

jobs:
  auto-label:
    runs-on: ubuntu-latest

    permissions:
      contents: read
      pull-requests: write

    steps:
      - name: Run Labeler
        uses: actions/labeler@v5
        with:
          configuration-path: .github/configs/labeler.yml
          sync-labels: true
